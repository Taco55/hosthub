# Agent Notes

- Gebruik waar mogelijk `styled_widgets` componenten in plaats van standaard Flutter-widgets.
- Voor tabellen en overzichten heeft `StyledDataTable` de voorkeur.
- Voor knoppen en toggles hebben `StyledButton`, `StyledIconButton` en `StyledSegmentedControl` de voorkeur.
