HostHub Console Theme Guide
===========================

Moved to `.agents/skills/hosthub-styling/SKILL.md` (reachable as the
`hosthub-styling` skill), so agents load it automatically and there is one copy
rather than two that drift apart.
