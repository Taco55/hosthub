name = "hosthub-admin-router"
main = "src/worker.js"
compatibility_date = "2026-02-18"
workers_dev = true

# Keep the route managed from this repo.
routes = [{ pattern = "www.trysilpanorama.com/admin*", zone_name = "trysilpanorama.com" }]

[assets]
directory = "../../hosthub/hosthub_console/build/web"
binding = "ASSETS"
